@'
# Manual Test Plan

| id | tool | setup | input | expected | result | evidence |
|---|---|---|---|---|---|---|
| TC-01 | add_task | Reset `data/todos.json` to the known fixture state. Use `examples/addTask.json`. | `{"title":"Finish MCP Week 3 task","description":"Test adding a task through MCP Inspector"}` | Tool accepts the input, creates a new task, returns the created task, and persists it to `data/todos.json`. | PASS | [Screenshot](evidence/TC-01-add-task.png) |
| TC-02 | add_task | Use MCP Inspector with the server running. | `{"title":" ","description":"Invalid empty title"}` | Input is rejected by Zod validation because a whitespace-only title is invalid. No task is created. | FAIL -> PASS | Initially accepted whitespace-only title. Fixed by commit `0cb10de` (`fix: reject whitespace-only task titles`). Re-ran in Inspector and received `Input validation error`. [Screenshot](evidence/TC-02-validation.png) |
| TC-03 | list_tasks | Reset `data/todos.json` to the known fixture state. Use `examples/listTasks.json`. | `{}` | Tool returns the tasks from `data/todos.json` and returns a valid JSON response. | PASS | [Screenshot](evidence/TC-03-list-tasks.png) |
| TC-04 | complete_task | Reset `data/todos.json` to the known fixture state. Use a task ID that does not exist. | `{"id":"999"}` | Tool returns a short actionable error and does not modify the data file. | PASS | MCP Inspector result: `Could not complete the task. Check the task ID and try again.` |
| TC-05 | complete_task | Reset `data/todos.json` so task ID `1` exists and is open. Use `examples/completeTask.json`. | `{"id":"1"}` | Tool finds the task, marks it as completed, returns the updated task, and persists the change to `data/todos.json`. | PASS | [Screenshot](evidence/TC-05-complete-task.png); `data/todos.json` confirmed the persisted change. |
| TC-06 | complete_task | Reset `data/todos.json` to the known fixture state. Use a task ID that does not exist. | `{"id":"999"}` | Tool returns a short actionable error and does not modify the data file. | PASS | MCP Inspector result: `Could not complete the task. Check the task ID and try again.` |
| TC-07 | list_tasks | Temporarily replace `data/todos.json` with an empty task array, then restore the fixture after the test. | `{}` | Tool returns an empty `items` array and the message `No tasks found.` | PASS | [Screenshot](evidence/TC-07-empty-tasks.png) |
| TC-08 | get_post | Use MCP Inspector with the server running. Simulate an API error using a post ID that does not exist. | `{"id":999999999}` | The API request fails, the error is handled, and the tool returns the short actionable error: `Could not fetch the post. Check the post ID and try again.` | PASS | [Screenshot](evidence/TC-08-get-post-error.png) |
'@ | Set-Content docs/test-plan.md -Encoding utf8